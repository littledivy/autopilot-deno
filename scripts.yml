scripts:
  bench:
    cmd: deno run --unstable -A benchmarks/index.ts
    env:
      DEV: true
      DEBUG: true
  test:
    cmd: deno run --unstable -A examples/type.ts
    env:
      DEV: true
      DEBUG: true
  fmt: deno fmt && cargo fmt
  build: cargo build
